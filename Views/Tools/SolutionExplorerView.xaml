<UserControl x:Class="WPFBase.Views.Tools.SolutionExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button ToolTip="New File" Padding="2" 
                    IsEnabled="{Binding HasSelection}">
                <TextBlock Text="📄" FontSize="14"/>
            </Button>
            <Button ToolTip="New Folder" Padding="2"
                    IsEnabled="{Binding HasSelection}">
                <TextBlock Text="📁" FontSize="14"/>
            </Button>
            <Separator/>
            <Button ToolTip="Refresh" Padding="2">
                <TextBlock Text="🔄" FontSize="14"/>
            </Button>
            <Button ToolTip="Collapse All" Padding="2"
                    IsEnabled="{Binding Files, Converter={StaticResource IsNotNullConverter}}">
                <TextBlock Text="⊟" FontSize="14"/>
            </Button>
        </ToolBar>
        
        <!-- File Tree -->
        <TreeView Grid.Row="1" 
                  ItemsSource="{Binding Files}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Icon}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Size, Converter={StaticResource FileSizeConverter}}" 
                                   Margin="10,0,0,0" 
                                   Foreground="Gray"
                                   Visibility="{Binding IsFile, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="True"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PrimaryColorBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>