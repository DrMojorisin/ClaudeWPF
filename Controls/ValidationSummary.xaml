<UserControl x:Class="WPFBase.Controls.ValidationSummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFBase.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Border x:Name="ValidationBorder" 
            BorderBrush="Red" 
            BorderThickness="1" 
            CornerRadius="4" 
            Margin="5"
            Visibility="Collapsed">
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <Path Width="16" Height="16" 
                      Fill="Red" 
                      Data="M12,2C13.1,2 14,2.9 14,4C14,5.1 13.1,6 12,6C10.9,6 10,5.1 10,4C10,2.9 10.9,2 12,2M21,9V7L15,1H5C3.89,1 3,1.89 3,3V21A2,2 0 0,0 5,23H19A2,2 0 0,0 21,21V9M19,9H14V4H15.5L19,7.5V9Z"
                      VerticalAlignment="Center"/>
                <TextBlock Text="Please correct the following errors:" 
                          FontWeight="Bold" 
                          Foreground="Red" 
                          Margin="5,0,0,0"
                          VerticalAlignment="Center"/>
            </StackPanel>
            <ItemsControl x:Name="ErrorsList" Margin="10,0,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="•" Foreground="Red" Margin="0,0,5,0" VerticalAlignment="Top"/>
                            <TextBlock Text="{Binding}" 
                                      Foreground="Red" 
                                      TextWrapping="Wrap"
                                      VerticalAlignment="Top"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>